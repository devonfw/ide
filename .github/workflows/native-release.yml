name: GraalVM Native Image builds
on: push
jobs:
  build-natives:
    name: Build native images
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest, ubuntu-latest, macos-latest]

    steps:
      - uses: actions/checkout@v3
        with:
          ref: feature/release-native-images
          
      - uses: graalvm/setup-graalvm@v1
        with:
          java-version: '17.0.7'
          distribution: 'graalvm'
          github-token: ${{ secrets.GITHUB_TOKEN }}
          native-image-job-reports: 'true'
          
      - name: Build native images with maven plugin
        shell: bash
        run: |
          cd ide
          mvn -B -X -Pnative -DskipTests=true package
          cd package/target
          tar -cvf ide-${{ runner.os }}.tar devon*
          ls -lR

      - uses: actions/upload-artifact@v3
        with:
          name: natives
          path: ide/package/target/ide*.tar

  attach-artifacts:
    name: Attach native images and create release
    runs-on: ubuntu-latest
    needs: [build-natives]
    
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: recursive
      - name: Set up JDK
        uses: actions/setup-java@v1
        with:
          java-version: '17'
      - uses: actions/download-artifact@v3
      - name: Create tar.gz archives for release
        run: |
          mv ./natives/ide-windows.tar ./ide/package/natives/ide-windows.tar
          tar -rvf ./ide/package/natives/ide-windows.tar ./natives/devon.exe ./scripts/target/package/system ./scripts/target/package/devonfw-ide-doc.pdf
          gzip ./ide/package/natives/ide-windows.tar
          mvn -B -ntp -DskipTests=true -Dstyle.color=always install
          ls -R
          gh release create v1.0.1 ./ide/package/natives/*.tar.gz
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
